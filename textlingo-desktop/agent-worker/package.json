{
  "name": "textlingo-agent-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "stage": "npm run build && node stageWorker.mjs",
    "test": "vitest run && node --test stageWorker.test.mjs",
    "test:bundle": "vitest run --exclude src/mindMapTask.test.ts && node --test stageWorker.test.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@opencode-ai/sdk": "^1.2.20",
    "opencode-ai": "1.2.20",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
